import { Router } from "express";

import bitrixRoutes from "./bitrix.routes.js";
import clientesRoutes from "./clientes.routes.js";

////////////////////////////////////////////////////////////

const router = Router();

////////////////////////////////////////////////////////////
// RUTA PRINCIPAL
////////////////////////////////////////////////////////////

router.get("/", (req, res) => {
    res.status(200).json({
        ok: true,
        message: "API REST Bitrix24 + MySQL funcionando correctamente"
    });
});

////////////////////////////////////////////////////////////
// RUTAS MODULARES
////////////////////////////////////////////////////////////

router.use("/bitrix", bitrixRoutes);

////////////////////////////////////////////////////////////

router.use("/clientes", clientesRoutes);

////////////////////////////////////////////////////////////

export default router;